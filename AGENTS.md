# Agent Instructions

These instructions apply to AI agents working in this repository.

## Start Here

- Use `README.md` for the project overview, install flow, quick start, and primary links.
- Use `ARCHITECTURE.md` for repository layout, package responsibilities, entry points, and build flow.
- Use `CONTRIBUTING.md` for development setup, tests, PR expectations, and documentation update requirements.
- Use `docs/index.md` to find the canonical user-facing documentation file.
- Use `docs/architecture.md` for runtime behavior, component interactions, and data flow.

## Documentation Sync

Use `.agents/skills/documentation-sync/SKILL.md` for branch-to-`main` documentation sync workflow and `docs/index.md` to choose the canonical doc to update.

## Context Budget

Keep this file as routing guidance only. Do not duplicate details already covered by `README.md`, `ARCHITECTURE.md`, `CONTRIBUTING.md`, or `docs/`.

## Verification Expectations

- For code changes, run relevant tests from `CONTRIBUTING.md`.
- For documentation-only changes, verify links and review the final diff.
- Never claim work is complete without checking the files you changed.
